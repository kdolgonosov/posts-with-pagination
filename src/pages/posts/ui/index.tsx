import { PostCatalog } from 'widgets/PostCatalog';
import { Layout } from 'shared/ui/Layout/Layout';

export const PostsPage = () => {
    return (
        <Layout>
            <PostCatalog />
        </Layout>
    );
};
